# Backend origin only (no trailing slash, no /api). Required for production builds
# when the API is on another host (e.g. Vercel frontend + Render backend).
# Example: https://your-timetable-api.onrender.com
REACT_APP_API_URL=
